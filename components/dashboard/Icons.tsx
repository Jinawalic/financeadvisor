/**
 * Icons.tsx — Centralized icon re-exports using lucide-react.
 * All icon names here mirror the Heroicons naming convention used
 * across the dashboard components so existing imports stay unchanged.
 */

export {
    // ChatArea
    SendHorizontal as PaperAirplaneIcon,

    // Sidebar nav items
    Plus          as PlusIcon,
    MessageSquare as ChatBubbleLeftRightIcon,
    Settings      as CognitionIcon,
    LogOut        as ArrowLeftStartOnRectangleIcon,
} from 'lucide-react';
